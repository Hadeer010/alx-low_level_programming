#include "main.h"
/**
 * add - is a fnction that adds two integers and returns the result.
 * @x: is the first number
 * @y: is the seconf number
 * Return: the number equal to the sum of x and y
 */
int add(int x, int y)
{
	return (x + y);
}
