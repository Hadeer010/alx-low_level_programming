#include "main.h"
/**
 * _isdigit - hecks for a digit (0 through 9).
 * @cl: input
 * Return: 1 if successful c is a digit, 0 if isn't a digit
 */
int _isdigit(int c)
{
	int cl;
	if (cl >= 48 & cl <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
